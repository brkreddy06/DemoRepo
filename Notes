https://www.youtube.com/watch?v=UF8prUYlA98&list=PLFGoYjJG_fqq6cHeqfsDes3pdVh3kpl74&index=10

Commands:
>git init --> it will initialize the .git file which is hidden (ex: goto C:\Ram\Selenium\Project\DemoGITRepo path in cmd and enter git init)
>git remote add origin https://github.com/brkreddy06/DemoRepo.git --> to establish the connection from our local machine to git
>git status --> to check the status of the untracked files or not commited files
>git add . --> to reay to checkin (push) complete project folder into git or if u want to add 2 files (ex:git add HomePage.java) 
>git commit -m "<put any comments>"--> to push the code into git 
>git clone --> is used to clone the exact repository from git to local machine 
>git pull origin master --> is used to get the latest code version
******************************PUSH Steps**********************
pull to git repository
stepts:
>git init
>git remote add origin <url>
>git status
>git add .
>git status
>git commit -m "comments"
>git push origin master
******************************PULL Steps************************
push to local machin from git repository
steps:
>git clone
>git pull origin master


